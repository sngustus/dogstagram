<h1>Posts</h1>

<table>
  <% @posts.each do |post| %>
    <tr>
      <td><%= image_tag(post.image) %></td>
      <td><%= post.caption %></td>
    <tr>
  <%end%>
</table>

<br />
<%= link_to 'New Post', new_post_path %>